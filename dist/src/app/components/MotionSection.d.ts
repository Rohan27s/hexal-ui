import { ReactNode } from 'react';
interface MotionSectionProps {
    children: ReactNode;
}
declare const MotionSection: ({ children }: MotionSectionProps) => import("react").JSX.Element;
export default MotionSection;
