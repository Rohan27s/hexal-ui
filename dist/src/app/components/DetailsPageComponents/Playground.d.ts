import React from 'react';
declare const Playground: ({ variant, size, shape, disabled, animate, setVariant, setSize, setShape, setDisabled, setAnimate }: any) => React.JSX.Element;
export default Playground;
